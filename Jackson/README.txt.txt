currently viewable at people.eecs.ku.edu/~jschilmo/integrate.html

working on:
drawIntegral function
polynomial + bounds + number of subintervals -> tuples ;parser


right now, this function only takes in tuples, so integrating (heh) it into our project may not be immediately feasible.
all that is needed is a basic parser like described above, am currently working on one